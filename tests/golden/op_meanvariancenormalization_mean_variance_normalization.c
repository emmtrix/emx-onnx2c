/*
 * Generated by emmtrix ONNX to C Compiler (emx-onnx2c)
 *
 * Command line: n/a
 * Model checksum (sha256): 82a856077fbf0dd624ed657f1888d1f8475339d8508f049c35b80444baa58e79
 * Model name: model
 * Graph name: meanvariancenormalization_graph
 * Inputs: 1 Outputs: 1 Nodes: 1 Initializers: 0
 * IR version: 7
 * Model version: n/a
 * Domain: n/a
 * Producer: onnx2c (version: n/a)
 * Opset imports: ai.onnx=13
 * Description:
 *   n/a
 * Graph description:
 *   n/a
 * Metadata:
 *   n/a
 */

#include <stddef.h>
#include <math.h>

/*
 * Node 0:
 * OpType: MeanVarianceNormalization
 * Inputs: in0
 * Outputs: out
 * Attrs:
 *   axes: [-1]
 */
static inline void model_op0(const float in0[restrict 2][3][4], float out[restrict 2][3][4]) {
    for (size_t i0 = 0; i0 < 2; ++i0) {
        for (size_t i1 = 0; i1 < 3; ++i1) {
            float sum = 0.0f;
            for (size_t i2 = 0; i2 < 4; ++i2) {
                sum += in0[i0][i1][i2];
            }
            float mean = sum / 4;
            float var = 0.0f;
            for (size_t i2 = 0; i2 < 4; ++i2) {
                float diff = in0[i0][i1][i2] - mean;
                var += diff * diff;
            }
            float denom = sqrtf(var / 4 + 1e-09f);
            for (size_t i2 = 0; i2 < 4; ++i2) {
                out[i0][i1][i2] = (in0[i0][i1][i2] - mean) / denom;
            }
        }
    }
}

void model(const float in0[restrict 2][3][4], float out[restrict 2][3][4]) {
    model_op0(in0, out);
}
